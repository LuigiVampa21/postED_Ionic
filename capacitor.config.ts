import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'postED_Ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
